
var Entity = function () {
  var x, y, mass, radius, velocity, angle;
  
  
  return {
    x: x,
    y: y,
    mass: mass,
    radius: radius,
    velocity: velocity,
    angle: angle
  };
};

module.exports = Entity;